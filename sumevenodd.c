#include<stdio.h>
int main()
{
    int num,i,sum=0,temp;
    printf("enter num");
    scanf("%d",&num);
    for(i==0;i<=num;i++){
        if(i%2==0){
        sum=sum+i;
        printf("sum of even is%d",sum);
        }
        else
        temp=temp+i;
        printf("sum of odd is",temp);
}
    }
}